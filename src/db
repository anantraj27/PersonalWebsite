
import { Pool } from 'pg';

import pg from "pg"
// const db = new Pool({
//   connectionString: process.env.DATABASE_URL,
//   ssl: { rejectUnauthorized: false }
// });

const db = new pg.Client({
  port: 5236,
  password: "24-CSE-25",
  database: "LOGIN",
  host: "localhost",
  user: "postgres"

})

db.connect()


export  default db ;